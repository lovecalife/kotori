# Codex 作業ガイド

このファイルと `README.md`、実際のソースコードを作業の基準にする。`.claude/` や `CLAUDE.md` は開発手順・設定・資料として参照しない。Claude 側のローカルデータが変更されても、このリポジトリの起動や作業手順に影響しない構成を維持する。

## 最初に確認する場所

- `README.md`: 起動手順、全体構成、データの所在。
- `index.html`: スクリプトの読み込み順。フロントエンドはグローバルスクリプトとブラウザー内 Babel で動く。
- `js/constants.js` と `js/utils.js`: カード CSV の取得・列名対応。
- `js/app.js` と `js/components/`: 画面・検索・デッキ操作。
- `js/deckStore.js`: 保存、移行、バックアップ。
- `js/sync.js`、`server/README.md`、`server/src/index.js`: 同期プロトコルとサーバー。

## 作業上の注意

- `node scripts/serve.mjs` でローカル画面を起動する。フロントエンドにビルド工程はない。
- ブラウザーの保存デッキはユーザーデータ。`localStorage` のキーやデッキ形式を変える場合は移行とバックアップ互換性を確認する。
- `index.html` のスクリプト順を変更する場合、先に定義されるグローバル名と JSX 変換の依存関係を確認する。
- カード CSV の列を変更する場合、`COLUMN_MAP` とメンバー / ライブ両方の表示・フィルターを確認する。
- 同期 API の変更はクライアント `js/sync.js` とサーバー `server/src/index.js` の両方を確認する。D1 スキーマ変更は `server/migrations/` に追加する。
- 認証情報、シンクキー、ブラウザーの保存デッキ、ローカル D1 データをコミットしない。
- 変更後は対象の JS の構文チェックと、影響する画面・API の動作確認を行う。既存の自動テストはない。
